# Lab 2 — Peer Review Record  (fill this in)

**Author:** <เบญญาภา> — <67070501030> — GitHub: @Piink7878

**Peer reviewer:** <กมนนัทธ์> — <67070501001> — GitHub: @Kamonnatt23

## Pull Requests I authored (reviewed by my partner)
| PR | Reviewer | Decision | Review Comment | My Response | Evidence Link |
|----|----------|----------|----------------|-------------|---------------|
| Feature: Sprint Engineering Specification & Test Plan (Issue 1) #24 | Kamonnatt23 | Request changes | รบกวนช่วยปรับแก้เอกสาร Specification เพื่อป้องกันความสับสนตอนนำไป Implement จริง 2 จุดดังนี้นะคะ 1. ขอให้ Clarify การใช้งาน X-Requester-Id กับ requesterId ให้ชัดเจนขึ้น: ตอนนี้ใน API spec ระบุว่า requester สามารถมาจาก X-Requester-Id หรือ requesterId ใน body ก็ได้ แต่ด้าน validation ระบุว่าทั้งสองค่าต้อง match กัน ซึ่งอาจทำให้ตอน implement งงว่าอันไหนเป็นตัวหลัก แนะนำให้กำหนดให้ชัดไปเลยว่า X-Requester-Id เป็น source of truth แล้ว requesterId ใน body ต้องตรงกับ header (ถ้าจะเก็บไว้) หรือไม่ก็เลือกใช้แค่ทางใดทางหนึ่ง จะได้ไม่เกิดความเข้าใจไม่ตรงกันตอนทำ API ค่ะ 2. Query parameter ของ My Tickets ยังไม่ตรงกันระหว่างไฟล์ specification.md กับ api-spec.md: ใน api-spec.md ใช้ requestedPriority, itPriority, pageSize แต่ใน specification.md ตรง API Contract Summary ยังใช้ priority, limit อยู่ แนะนำให้เลือกชื่อให้เหลือชุดเดียวแล้วแก้ให้ตรงกันทั้งสองไฟล์เลยค่ะ เพราะถ้าปล่อยไว้ตอน frontend กับ backend นำไป implement อาจจะใช้ parameter คนละชื่อกันได้ค่ะ | - | https://github.com/Piink7878/cpe334-TokTikIT/pull/24 |
| Feature: Database Schema Increment & Seed Data (Issue 2) #25 | Kamonnatt23 | Request changes | ขอแก้ seed.ts ตรง flow ของ main() นิดนึงนะคะ 1. ตอนนี้เหมือน prisma.$disconnect() ใน finally ถูกเรียกก่อนที่จะ seed RelatedSystem กับ DevelopmentRequester เสร็จ 2. ด้านล่างมีการเรียก main() ซ้ำ 2 รอบด้วย (main(); และ main().catch(...)) ซึ่งอาจทำให้ seed พยายามใช้ Prisma หลังจาก disconnect ไปแล้ว และทำให้การรัน seed ไม่ได้เป็นไปตามที่ตั้งใจ แนะนำให้เหลือเรียก main() แค่ครั้งเดียว แล้วใช้ try/catch/finally ครอบการ seed ทั้งหมด Categories, Related Systems และ Development Requesters ทั้งหมดให้เสร็จก่อน แล้วค่อย $disconnect() ทีเดียวค่ะ นอกนั้น schema กับ migration โดยรวมดูโอเคเลย 👍 | แก้ไขโค้ดให้รวบไว้ใน try/catch เดียว และลบการเรียก main() ที่ซ้ำซ้อนออกเรียบร้อยแล้ว รบกวนรีวิวอีกรอบค่าา | https://github.com/Piink7878/cpe334-TokTikIT/pull/25 |
| Feature: Development Requester Selector & Context (Issue 3) #26 | Kamonnatt23 | Request changes | ขอเพิ่ม persistence ให้ RequesterContext อีกนิดนึงนะคะ 🙏 1. ตอนนี้ selectedRequester เก็บด้วย useState อย่างเดียว ซึ่งทำให้ context อยู่ได้ตอน navigate ระหว่างหน้า แต่ถ้า user กด refresh (F5) หน้าเว็บ selectedRequester จะกลับเป็น null แล้ว ProtectedRoute ก็จะ redirect กลับไปหน้าเลือก requester ใหม่ 😅 2. เนื่องจาก requirement ของ Issue 3 เป็นการเก็บ Requester Context แนะนำให้ persist ค่า requester ไว้ใน localStorage หรือ sessionStorage แล้วตอน RequesterProvider mount ให้โหลดค่ากลับมา จะได้รักษา requester context หลัง refresh ด้วยค่ะ ส่วน API /api/requesters กับหน้า Selection และ Change Requester โดยรวมดูโอเคแล้ว 👍 | เพิ่ม persistence ด้วย localStorage เข้าไปใน RequesterProvider แล้ว ตอนนี้กด F5 หรือ refresh หน้าเว็บแล้ว Context ไม่หลุดแล้ว และตอนกด Change Requester ก็เคลียร์ storage ออกให้ปกติเลย รบกวนรีวิวอีกรอบนะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/26 |
| Feature: Ticket Creation Feature API & UI (Issue 4) #27 | Kamonnatt23 | Request changes | ขอเพิ่มข้อเสนอแนะ 2 จุดนะคะ 1. ขอเช็กเรื่อง Ticket Number uniqueness เพิ่มนิดนึงค่ะ ตอนนี้ logic จะ findFirst() หา ticket ล่าสุด → เอา sequence +1 → แล้วค่อย create ticket ซึ่งถ้ามี request เข้ามาสร้างพร้อมกัน 2 ตัว อาจมีโอกาสที่ทั้งสอง request อ่านเลขล่าสุดตัวเดียวกัน แล้ว generate Ticket Number เดียวกันได้ เนื่องจาก requirement ระบุว่า Ticket Number ต้อง unique อัตโนมัติ แนะนำลองพิจารณาวิธีที่รองรับ concurrent request ด้วย เช่นใช้ DB sequence/transaction หรือมี unique constraint + retry กรณีเลขชนกันค่ะ 2. ขอเพิ่ม edge cases สำหรับ Test ของ Create Ticket อีกนิด ตอนนี้มี test พวก success, missing requester header, summary สั้น และ category ไม่พบแล้ว 👍 แต่เพื่อให้ครอบคลุมตาม feature มากขึ้น อยากให้เพิ่ม test พวก relatedSystemId ไม่พบ, invalid priority, requester ID mismatch และ boundary ของ summary/description ด้วยค่ะ นอกจากนี้ส่วน attachment ตอนนี้มีการจำกัด 5 ไฟล์, 5MB และ allowed MIME type ใน upload middleware แล้ว แต่ test ยัง mock upload ให้ผ่านตลอด เลยยังไม่ได้ verify พวก file type, file size และจำนวนไฟล์จริง ๆ ถ้าเพิ่มส่วนนี้ได้จะช่วยให้ test coverage ของ Issue 4 แน่นขึ้นมากเลยค่ะ | แก้ไขระบบรันเลขให้รองรับ Concurrency ด้วย Retry Logic + Unique Constraint แล้ว พร้อมทั้งเพิ่ม Test Cases คลุม Edge Cases และยิงไฟล์จริงผ่าน Supertest เรียบร้อยแล้ว รบกวนรีวิวอีกรอบนะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/27 |
| Feature: My Tickets List & Search (Issue 5) #28 | Kamonnatt23 | Request changes | โดยรวม implementation ของ My Tickets ทำมาครบดี ทั้ง ownership, search, filter, sort, pagination รวมถึง loading / empty / no-results state 👍 แต่มีเรื่อง Test Coverage ที่อยากให้เพิ่มอีกนิดนะคะ 1. ตอนนี้ my-tickets.api.test.ts มี test หลัก ๆ แล้ว แต่ยังไม่ได้แยก test เพื่อยืนยัน behavior ของแต่ละส่วนอย่างชัดเจน อยากให้แยก test เช่น Search แล้วได้เฉพาะ ticket ที่ match, Filter ตาม status/category/priority, Sort ทั้ง asc และ desc, และ Pagination หลายหน้า (รวมถึง hasNextPage/hasPreviousPage) ค่ะ 2. อยากให้เพิ่ม Frontend interaction test ด้วยว่าเมื่อ UI มีการเปลี่ยน Search / Filter / Sort / Pagination แล้ว getTickets() ถูกเรียกด้วย parameters ที่ถูกต้อง เพราะตอนนี้มีแค่ combined query test ที่ส่ง parameters หลายตัวพร้อมกัน ซึ่งอาจจะยังไม่ครอบคลุม behavior ของแต่ละ feature รบกวนเพิ่ม test coverage ส่วนนี้อีกนิด เพื่อให้ทุก Acceptance Criteria ของ Issue มี test รองรับครบถ้วนนะคะ | เราแก้และ รบกวนเข้ามาตรวจอีกรอบบ | https://github.com/Piink7878/cpe334-TokTikIT/pull/28 |
| Feature: Requester Ticket Detail & Attachment Lifecycle (Issue 6) #29 | Kamonnatt23 | Request changes | ขอเช็กเพิ่มเติม 2 จุดนะคะ 1. ขอเพิ่ม requester validation ใน Attachment API อีกนิด ตอนนี้ POST /api/tickets/:id/attachments, GET /api/attachments/:id/download และ DELETE /api/attachments/:id เช็กว่า X-Requester-Id มีค่าและเป็นตัวเลขแล้ว แต่ยังไม่ได้เช็กว่า requester มีอยู่จริง / active ใน DB ก่อนทำ ownership check เช่น ถ้าส่ง X-Requester-Id: 99999 ที่ไม่มีอยู่จริง ตอนนี้จะไหลไปถึง ownership check แล้วอาจกลายเป็น 403 ซึ่งไม่ค่อย consistent กับ API contract แนะนำให้เพิ่ม requester existence/active check ให้ Attachment endpoints ด้วยจะได้เหมือน endpoint อื่น ๆ ค่ะ 2. สำหรับ Attachment tests ตอนนี้ทำมาครบหลายเคสแล้ว 👍 แต่อยากให้เพิ่ม boundary/failure cases อีกนิด ที่อยากให้เพิ่มคือกรณีขนาดไฟล์ exactly 5MB ต้อง upload ได้ เพื่อเช็ก boundary ของ limit และเพิ่มเคส ticket not found / attachment not found อีกนิด จะได้ครอบคลุม failure cases ของ lifecycle มากขึ้นค่ะ | แก้แล้ววว รบกวนตรวจอีกรอบค่า | https://github.com/Piink7878/cpe334-TokTikIT/pull/29 |
| Feature: Responsive UI Verification & E2E Testing (Issue 7) #30 | Kamonnatt23 | Request changes | เยี่ยมไปเลย แต่ขอคอมเม้นนิดนึงนะคะ 1. E2E flow ตอนนี้ไปถึง Ticket Detail แล้ว แต่ยังไม่ได้ cover Attachment flow ตอนนี้ test ครบถึง Select Requester → Create Ticket → Search My Tickets → Open Ticket Detail แล้ว แต่ใน Lab 2 product flow ยังมี Attachment lifecycle ด้วย ไม่ต้อง implement attachment ซ้ำใน Issue 7 นะคะ แนะนำแค่เพิ่ม E2E step ที่ Ticket Detail เพื่อ verify attachment ที่ทำไว้ใน Issue 6 เช่น upload → แสดง metadata → download/remove ตาม scope ที่ทำไว้ จะได้เป็น end-to-end flow ที่ครอบคลุมมากขึ้นค่ะ 2. Responsive screenshot มีครบแล้ว แต่ขอเพิ่ม visual verification อีกนิด ตอนนี้มี screenshot ครบ Desktop / Tablet / Mobile แล้ว ถือว่าดีมาก แต่ Lab 2 ยังต้องมี visual checklist ตรวจพวก horizontal overflow, clipping, overlap, hidden buttons และ attachment names ด้วย แนะนำเพิ่ม checklist หรือ automated assertion เล็ก ๆ เช่นเช็ก scrollWidth <= clientWidth และองค์ประกอบสำคัญไม่ overflow ในแต่ละ viewport จะได้พิสูจน์ว่า responsive ไม่ใช่แค่มี screenshot แต่ layout ใช้งานได้จริงค่ะ | ทำเพิ่มเร่บร้อย ช่วยเช็คด้วยยย | https://github.com/Piink7878/cpe334-TokTikIT/pull/30 |


## Pull Requests I reviewed for my partner

| Partner PR | Decision | My Comment | Partner Response | Evidence |
|------------|----------|------------|------------------|----------|
| docs: add lab 2 specification, test plan, ui and api specs #21 | Request changes | โดยรวมโอเคเลย แต่มีจุดที่อยากให้แก้ก่อน merge นิดหน่อยนะคะ 1. POST /api/attachments กับ /api/attachments/upload ในแต่ละ spec ไม่ตรงกัน รบกวนเลือกให้เหลืออันเดียว 2. Related System ระบุว่า required ในบางที่ แต่ใน BR-02 ไม่ได้ใส่ไว้ รบกวนทำให้ตรงกัน 3. GET /api/attachments/:id/download มีใน api-spec.md แต่ไม่มีใน specification.md น่าจะเพิ่มให้ครบ 4. Test ยังไม่ค่อยครอบคลุมพวก pagination/filter/sort และกรณี user พยายามเข้าถึง ticket ของคนอื่น โดยเฉพาะเรื่อง user isolation น่าจะเพิ่ม test ไว้หน่อย 5. Flow ของ attachment ที่ upload ก่อนสร้าง ticket ยังไม่ค่อยชัด โดยเฉพาะกรณีกด Remove ก่อน submit อยากให้ระบุ behavior ไว้นิดนึง นอกนั้นโอเคเลยย เย่ ๆ | - | https://github.com/Kamonnatt23/toktickit/pull/21 |
| feat: implement development requester context #22 | Request changes | อยากให้แก้เพิ่มนิดหน่อยนะคะ 1. ถ้า requester ใน localStorage เคยเลือกไว้ แต่ภายหลังถูกเปลี่ยนเป็น inactive ตอนนี้ยังสามารถ restore user เดิมได้อยู่ อยากให้เช็กกับ active requester จาก API ก่อน restore 2. เพิ่ม test กรณี API fail, ไม่มี active requester, loading และ localStorage เป็น user ที่ไม่ valid/stale หน่อย จะได้ครอบคลุม edge case ตาม requirement 3. กรณีไม่มี active requester ตอนนี้ dropdown จะว่าง ๆ อยากให้มี empty state/message บอกชัด ๆ ว่าไม่มี active requester 4. API test ตอนนี้ค่อนข้างผูกกับ seed เช่น length >= 4 แนะนำให้เน้น test behavior ว่า API คืนเฉพาะ active requester มากกว่าค่ะ นอกนั้น flow หลักโอเคแล้ว 👍 | แก้ไขเรียบร้อย เข้ามาตรวจอีกรอบหน่อย | https://github.com/Kamonnatt23/toktickit/pull/22 |
| Feature/3 ticket creation #23 | Request changes | มีจุดที่อยากให้แก้เพิ่มให้ครบตาม lab spec นิดหน่อยนะคะ 1. ตอนนี้ Create Ticket ยังไม่มี Attachment ทั้งที่ต้องรองรับ type/size/max files ตาม spec 2. หลังสร้างสำเร็จควรเอา official Ticket Number จาก backend มาแสดง ไม่ใช่แค่ “Ticket created successfully!” 3. Validation ตอนนี้เช็กแค่ required อยากให้เพิ่ม length / trim / allowed values และ backend validation ด้วย 4. ถ้าโหลด Category / Related System ไม่สำเร็จ ตอนนี้ยังไม่มี error state ให้ user เห็น 5. ยังไม่เห็น tests ของ API/UI สำหรับ create ticket อยากให้เพิ่ม happy path, invalid/boundary และ API failure cases ด้วยค่ะ โดยรวม flow หลักโอเคแล้ว เหลือเก็บพวก requirement กับ test coverage ให้ครบค่ะ 👍 | ขอบคุณสำหรับรีวิวค่ะ สำหรับข้อ 1 เรื่องระบบ Attachment เราได้แพลนแยกไปทำใน Issue 6: Attachment management เพื่อให้โค้ดเป็นสัดส่วนและโฟกัสทีละเรื่อง ใน Issue นี้จะยังไม่มีส่วนของไฟล์นะคะ ส่วนข้อ 2-5 เห็นด้วยเลยค่ะ เลยได้แก้ไขเรียบร้อยแล้ว สรุปสิ่งที่มีการแก้ไขไปดังนี้นะคะ 1. เพิ่มหมายเลขตั๋วอย่างเป็นทางการ (Official Ticket Number): ฝั่ง Backend (app.ts) ปรับ API POST /api/tickets ให้มีการสร้างหมายเลขตั๋วอัตโนมัติจาก ID ในฐานข้อมูล (เช่น TKT-001) และส่งกลับมาเป็น Response ส่วนฝั่ง Frontend (CreateTicket.tsx) อัปเดตข้อความแจ้งเตือนสำเร็จให้แสดงหมายเลขตั๋วอย่างชัดเจน เช่น "Ticket TKT-001 created successfully!" 2. เพิ่มความเข้มงวดในการตรวจสอบข้อมูล (Strict Validation): ใส่คำสั่ง .trim() ทั้งหน้าบ้านและหลังบ้านเพื่อป้องกันการพิมพ์แค่ช่องว่าง, จำกัดความยาวของ Summary ไม่เกิน 100 ตัวอักษร และ Description ไม่เกิน 1000 ตัวอักษร รวมถึงล็อกค่า Priority ให้ส่งมาได้แค่ Low, Medium, High, หรือ Critical เท่านั้น (หากส่งค่าแปลกๆ มา API จะปฏิเสธทันที) 3. รองรับกรณี Dropdown โหลดข้อมูลพัง (API Error States): หาก API ของหมวดหมู่ (Category) หรือระบบที่เกี่ยวข้อง (Related System) โหลดไม่สำเร็จ จะแสดงกล่องแจ้งเตือน Error สีแดงให้ผู้ใช้ทราบทันทีที่ด้านบนของฟอร์ม 4. เพิ่มความครอบคลุมของการทดสอบ (Test Coverage): ฝั่ง Client เพิ่มเคสจำลองการกรอกตัวอักษรเกินกำหนด (Boundary cases), เคส API Dropdown โหลดพัง และเคสทดสอบการแสดงผล Ticket Number อย่างละเอียด ส่วนฝั่ง Server เพิ่มเคสยิง API โดยตรงเพื่อทดสอบการตีกลับ (Error 400) หากส่งข้อมูลยาวเกินไป, ส่งค่า Priority ผิด, หรือข้อมูลไม่ครบค่ะ รบกวนรีวิวดูอีกรอบนะคะ ขอบคุณค่ะ | https://github.com/Kamonnatt23/toktickit/pull/23 |
| feat: implement My tickets and unified navigation bar #24 | Request changes | มีจุดที่อยากให้เช็กเพิ่มนิดหน่อยนะคะ 1. ใน App.tsx ตอนนี้ <CreateTicket /> ถูก render ทั้งก่อนและใน {activeTab === 'create' ? ...} ทำให้หน้า Create Ticket น่าจะขึ้น form ซ้ำ 2 รอบ ตรงนี้น่าจะลบตัวที่ซ้ำออกค่ะ 2. Test ที่ชื่อ filters by status and sorts ตอนนี้เช็กแค่ status filter ยังไม่ได้ verify การ sort จริง ๆ แนะนำเพิ่ม test สำหรับ sort asc/desc ให้ครบตาม feature ที่ทำค่ะ 3. Pagination มี implementation แล้ว แต่ยังไม่เห็น test สำหรับ Next/Previous แนะนำเพิ่ม test ตรงนี้ด้วย จะได้ verify ว่าเปลี่ยน page และเรียก API ถูกค่ะ 4. ถ้ามีเวลาแนะนำเพิ่ม API error case ของ My Tickets ด้วย ตอนนี้มี UI error state แล้วแต่ test ยังไม่ได้ cover ค่ะ 5. รบกวนฝากแก้ให้เติม Navbar ที่มีโลโก้ TokTickIT, เมนูนำทาง และจุดที่แสดงชื่อ Requester ปัจจุบันพร้อมปุ่มสลับ User ตาม Requirement ด้วยนะคะ ส่วน ownership, search, pagination ฝั่ง API และ loading/empty state โดยรวมดูโอเคแล้วค่ะ | แก้ไขเรียบร้อย เข้ามาตรวจหน่อยค้าบ | https://github.com/Kamonnatt23/toktickit/pull/24 |
| feat: implement read-only ticket detail view and ownership validation… #25 | Request changes | มี 2 จุดที่อยากให้แก้เพิ่มเติมนะคะ 1. ใน App.tsx ตอนนี้ <MyTickets /> ถูก render ทั้งใน ternary เดิมและใน {activeTab === 'list' && ...} ทำให้ตอนอยู่หน้า My Tickets มีการ render ซ้ำ และตัวแรกไม่ได้ส่ง onTicketClick ไปด้วย แนะนำให้เหลือ conditional render แค่ชุดเดียวค่ะ 2. ฝั่ง API มี ownership check 403 แล้ว แต่ใน ticket-detail.api.test.ts ยังไม่เห็น test ที่ requester คนหนึ่งพยายามเปิด ticket ของ requester อีกคน แนะนำให้เพิ่มเคสนี้เพื่อ verify ownership requirement โดยตรงด้วยนะคะ | แก้ไขเรียบร้อยค่ะ | https://github.com/Kamonnatt23/toktickit/pull/25 |
| feat: implement attachment management (Issue 6) #26 | Request changes | มี 2 จุดที่อยากให้แก้ก่อน approve นะคะ 1. ตอนนี้ backend validate file type จาก mimetype อย่างเดียว แต่ requirement ระบุ allowed extensions เป็น JPG/JPEG/PNG/WEBP/PDF ด้วย แนะนำให้ validate extension เพิ่มด้วยค่ะ 2. ตอน soft delete ตอนนี้ TicketDetail เพิ่ม UI สำหรับแสดง attachment ที่ isDeleted แล้ว แต่ API /api/tickets/:id ยัง query เฉพาะ isDeleted: false ทำให้ deleted attachment ไม่ถูกส่งมาถึง UI เลย จึงไม่สามารถแสดงแจ้งเตือน Removed: ... ที่เพิ่มมาได้ ควรปรับให้ API กับ UI behavior ตรงกันค่ะ นอกจากนี้ในส่วนของ test อยากให้เพิ่มกรณีขนาดไฟล์ 5 MB พอดี, กรณีมีครบ 5 active attachments, empty removal reason และเช็ก ownership ตอน upload ด้วย จะได้ครอบคลุม requirement ของ Issue 6 มากขึ้นค่ะ | แก้แล้ว | https://github.com/Kamonnatt23/toktickit/pull/26 |
| fix(issue-7): Fix e2e tests, UI bugs and complete E2E testing #27 | Request changes | มี 2 จุดหลักและข้อเสนอแนะเพิ่มเติมที่อยากให้แก้นะคะ 1. ใน responsive.spec.ts ตอนนี้มีการเปลี่ยน viewport เป็น Desktop/Tablet/Mobile และถ่าย screenshot แล้ว แต่ยังไม่มี assertion ที่ตรวจว่า UI responsive จริง เช่น ไม่มี horizontal overflow หรือ element สำคัญยัง visible อยู่ ทำให้ test ตอนนี้พิสูจน์ได้แค่ว่าถ่าย screenshot ได้ แนะนำให้เพิ่ม responsive assertion เข้าไปด้วยค่ะ 2. ใน full-flow.spec.ts ตรงขั้นตอน Download ตอนนี้เช็กแค่ว่าปุ่ม Download มีอยู่ แต่ยังไม่ได้ verify ว่าการ download ทำงานได้จริง แนะนำให้ใช้คำสั่ง waitForEvent('download') ของ Playwright แล้วกดปุ่มเพื่อเช็กว่า download สำเร็จจริงค่ะ นอกจากนี้ ถ้าไฟล์ client/dummy.js ไม่ได้ใช้งานจริง แนะนำให้ลบออกจาก PR ด้วยนะคะ เพราะดูเหมือนเป็นไฟล์ทดลองมากกว่าไฟล์ที่จำเป็นกับโปรเจกต์ค่ะ | แก้แระ | https://github.com/Kamonnatt23/toktickit/pull/27 |