# Lab 3 — Peer Review Record

**Author:** เบญญาภา — 67070501030 — GitHub: @Piink7878

**Peer reviewer:** กมนนัทธ์ — 67070501001 — GitHub: @Kamonnatt23

## Pull Requests I authored (reviewed by my partner)

| PR | Reviewer | Decision | Review Comment | My Response | Evidence Link |
|----|----------|----------|----------------|-------------|---------------|
| Feature/1 Sprint 3 Engineering Contract, Specifications & Test Plan #52 | Kamonnatt23 | Request changes | โดยรวมเอกสารของ Issue 1 ค่อนข้างครบแล้ว ทั้ง `specification.md`, `api-spec.md`, `ui-spec.md` และ `tests.md` <br><br>มีจุดที่อยากให้เก็บรายละเอียดเพิ่มนิดหน่อยก่อน merge <br><br>* ใน `api-spec.md` อยากให้ระบุ **Status Transition Matrix** ให้ชัดขึ้นหน่อย ว่า status ไหนเปลี่ยนไป status ไหนได้ และ role ไหนมีสิทธิ์ทำได้<br>* ส่วนเรื่อง Authentication อยากให้ระบุรายละเอียดของ **session expiration / logout invalidation / CSRF consideration / safe error** ให้ครบตาม Engineering Contract มากขึ้น<br>* อยากเช็ก `tests.md` เพิ่มอีกนิดว่า **Acceptance Criteria ทุกข้อมี test รองรับครบหรือยัง** และมี coverage พวก authorization, migration/regression, responsive และ E2E ครบตาม scope หรือเปล่า<br><br>นอกนั้นโครงสร้างและ scope โดยรวมโอเคและ ถ้าเก็บรายละเอียด 2-3 จุดนี้เพิ่ม น่าจะพร้อมสำหรับ merge เข้า `lab3-staging`👍 | เราแก้แล้วเน้อ ฝากตรวจตรวจ | https://github.com/Piink7878/cpe334-TokTikIT/pull/52 |
| Feature/2 User Model Evolution, Migration & Seed Data #53 | Kamonnatt23 | Request changes | ตรวจ Issue 2 แล้ว โดยรวมโอเคเลย ทั้ง User model, relation กับ Ticket/Comment/Note, migration และ seed ครบตาม scope <br><br>มีจุดสำคัญที่อยากให้แก้นิดนึงก่อน merge ตรง migration ที่ย้าย `DevelopmentRequester` → `User`<br><br>ตอนนี้ `isActive` ใน User ถูกกำหนดเป็น `true` ตายตัว ทำให้ Requester ที่เดิมเป็น inactive อาจถูก migrate เป็น active ได้ อยากให้ตรงนี้ preserve ค่า `isActive` เดิมจาก `DevelopmentRequester` ไปเลย จะได้ไม่เสียข้อมูลเดิมและตรงกับ requirement เรื่อง migration<br><br>นอกนั้น seed ใช้ `upsert` และมี Active/Inactive ครบตามจำนวนที่กำหนดแล้ว <br><br>แก้จุดนี้แล้วเราโอเคให้ผ่าน Issue 2 ได้เลย | ขอบคุณที่ช่วยรีวิวและตรวจเช็กให้นะ<br><br>ตรงจุดของ isActive ในไฟล์ migration ตัวโค้ดปัจจุบันได้ทำการดึงค่า (Preserve) จาก "DevelopmentRequester" มาใส่ตรงๆ แล้วแต่อาจจะดูชวนสับสนนิดหน่อยเพราะบรรทัดมันเรียงติดกับค่า true ของ mustChangePassword<br><br>ถ้าดูจากลำดับ Column ของ INSERT INTO "User" จะจับคู่กันแบบนี้:<br>`INSERT INTO "User" ("id", "email", "passwordHash", "fullName", "role", "isActive", "mustChangePassword", "createdAt", "updatedAt")`<br>`SELECT gen_random_uuid(), "email", '$2a$10$wE/.c4Gk0u.O8x/2Hk/b3eA5gK1xN8n3L1u4N4s7O9n0B4q0P4U1G', "name", 'REQUESTER'::"Role", "isActive", true, "createdAt", "updatedAt" FROM "DevelopmentRequester";`<br><br>ถือว่า Issue 2 พร้อมที่จะ Merge แล้วอิอิ ขอบคุณน้า | https://github.com/Piink7878/cpe334-TokTikIT/pull/53 |
| Feature/3 Authentication API & Mandatory Password Change #54 | Kamonnatt23 | Request changes | เราไล่ดู Issue 3 แล้ว โดยรวมโครงสร้าง Auth มาถูกทางนะ ทั้ง Login, Change Password, AuthContext, ProtectedRoute และตัด `X-Requester-Id` ฝั่ง frontend ออกแล้ว <br><br>แต่มีจุดที่อยากให้แก้ก่อน merge:<br><br>1. **ProtectedRoute ยังมี logic ของ DevelopmentRequesterSelection ค้างอยู่**<br>   ตอนนี้ `ProtectedRoute` ยัง redirect ไป `/` ถ้าไม่มี `selectedRequester` แต่ Issue 3 เปลี่ยนมาใช้ authenticated user แล้ว และ `main.tsx` ก็เอา `RequesterProvider` ออกไปแล้ว อยากให้เอา dependency/logic ของ `selectedRequester` ออกจาก `ProtectedRoute` ให้หมด ไม่งั้น route อาจทำงานผิดหรือ redirect ไปหน้าที่ไม่ได้ใช้แล้ว<br><br>2. **App.tsx ยังไม่มี route `/` ที่ชัดเจน**<br>   ตอนนี้เอา `DevelopmentRequesterSelection` ออกแล้ว เหลือ `/login` เป็นหลัก ดังนั้นควรมี redirect จาก `/` ไป `/login` หรือกำหนด behavior ของ `/` ให้ชัดเจน จะได้ไม่เจอหน้า blank/404 ตอนเข้า root<br><br>3. **AuthContext ต้องเช็กว่า session ถูกส่งจริงทุก request ที่จำเป็น**<br>   ตอนนี้ `/me` ถูกเรียกจาก frontend แล้ว แต่ใน code ยังไม่ได้กำหนด `credentials: "include"` ชัดเจน อยากให้เช็กกับ backend/session setup ว่า cookie ถูกส่งในทุก API ที่ต้อง authenticate โดยเฉพาะกรณี frontend/backend คนละ origin<br><br>4. **ช่วยเพิ่ม/ยืนยัน test เรื่อง mandatory password change ให้ครบ**<br>   อยากให้ test ครอบคลุม flow นี้โดยตรง: login ด้วย initial password ที่ `mustChangePassword = true`, เข้า protected page ไม่ได้ และต้องถูกส่งไป `/change-password`, เปลี่ยน password สำเร็จแล้ว `mustChangePassword` ต้องเป็น `false`, หลังเปลี่ยนแล้วเข้า app ได้, user ที่ `mustChangePassword = false` เข้า app ได้ตามปกติ<br><br>5. **ต้องมี test สำหรับ inactive account**<br>   Lab 3 กำหนดว่า inactive user ต้อง authenticate ไม่ได้ อยากให้ `auth.api.test.ts` มี case นี้ชัด ๆ และเช็กด้วยว่าไม่สามารถเข้า `/me` หรือ protected API ต่อได้หลัง account ถูก inactive<br><br>6. **เช็ก logout/session invalidation**<br>   อยากให้ test ยืนยันว่า logout ไม่ใช่แค่ `setUser(null)` ฝั่ง frontend แต่ session ฝั่ง backend ถูก invalidate ด้วย และ request ที่ใช้ session เดิมหลัง logout ต้องเข้า protected API ไม่ได้<br><br>7. **เช็ก security ของ session cookie**<br>   จาก Lab 3 ที่เรากำหนดไว้ ต้องมีเรื่อง HttpOnly / Secure / SameSite และ session expiration ให้ตรงกับ API spec ด้วย อยากให้ implementation และ test ตรงกับที่เขียนไว้ใน Issue 1<br><br>8. **Regression ของ Requester flow**<br>   ใน PR นี้มีการเปลี่ยน API ของ ticket/attachment จากการส่ง `requesterId` มาเป็น authenticated identity ซึ่งเป็นทิศทางที่ถูกต้องตาม Lab 3 แต่ขอให้เช็กว่า Create Ticket / My Tickets / Ticket Detail / Attachment ยังทำงานครบหลังเปลี่ยน และไม่มี code เก่าที่พึ่ง `RequesterContext` เหลืออยู่<br><br>ถ้าแก้จุดพวกนี้แล้วเราโอเคให้ผ่าน Issue 3 ได้ | ขอบคุณสำหรับข้อเสนอแนะ ตอนนี้ได้ปรับปรุงตามคอมเมนต์ครบทุกประเด็นแล้ว:<br>- ลบ Logic เก่าของ DevelopmentRequesterSelection ออกจาก ProtectedRoute และเพิ่ม RootRedirect ใน App.tsx ตาม Role อย่างเป็นระบบ<br>- ใส่ credentials: "include" ให้ทุก API Call ฝั่ง Frontend เพื่อรับประกันการส่ง Session Cookie<br>- เพิ่ม Enforcement Middleware บล็อก (403) หากบัญชียังมีสถานะ mustChangePassword = true<br>- ลบ RequesterContext.tsx และ DevelopmentRequesterSelection.tsx ทิ้งทั้งหมด<br>- เพิ่ม Test Coverage ใน auth.api.test.ts ครอบคลุมเคส Inactive Account, Forced Password Change, Logout Invalidation, และ Session Security Flags เรียบร้อย<br>- ผลการรันเทสต์ผ่าน 100% ทั้ง API (8/8), Client (31/31) และ Playwright E2E (12/12)<br><br>ฝากตรวจสอบอีกรอบและช่วย Approve ให้หน่อยนะ! | https://github.com/Piink7878/cpe334-TokTikIT/pull/54 |
| Feature/4 Requester Identity Migration & Regression Fixes #55 | Kamonnatt23 | Request changes | เราเช็ก PR #55 ทั้ง diff และ backend flow แล้ว โดยรวมโอเคมาก โดยเฉพาะ Ticket/Attachment ตอนนี้ใช้ `req.user.id` จาก session จริงแล้ว และ regression tests ก็เปลี่ยนมาใช้ authenticated session ครบ <br><br>เหลือจุดเดียวที่อยากให้เก็บก่อน merge:<br><br>ตอนนี้ยังมี `GET /api/requesters` ใน `server/src/app.ts` และ `server/tests/lab-02/requesters.test.ts` อยู่ ซึ่งเป็น logic ที่มาจาก Development Requester selector โดยตรง<br><br>ใน Issue 4 scope ระบุว่าต้องลบ Development Requester selector ออกจากทั้ง Frontend และ Backend แล้ว ตอนนี้ frontend ไม่ได้ใช้ selector แล้ว แต่ backend endpoint นี้ยังอยู่ ถึง implementation จะเปลี่ยนมาอ่านจาก User แล้วก็ตาม<br><br>ถ้าไม่มี flow อื่นใช้ `/api/requesters` แล้ว อยากให้ลบ route + test ตัวนี้ออกด้วย จะได้ไม่มี legacy selector logic ค้างอยู่ใน backend<br><br>นอกจุดนี้เราเช็ก Ticket create / My Tickets / Ticket Detail / Attachment แล้วโอเคหมด และมี cross-user 403 regression test ครบ <br><br>แก้จุดนี้แล้วเราโอเคให้ผ่าน Issue 4 ได้เลย | ลบ GET /api/requesters ใน app.ts และลบไฟล์ server/tests/lab-02/requesters.test.ts ออกหมดจดแล้วจ้า ฝากเช็คหน่อยนะะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/55 |
| Feature/5 IT Staff Ticket Queue Screen & APIs #56 | Kamonnatt23 | Request changes | เราเช็ก PR #56 จาก commit `b0fde49` แล้ว ทั้ง API, Queue UI, route และ tests โดยรวมทำมาครบและมาถูกทางมาก <br><br>แต่ก่อน merge ขอให้แก้ 2 จุดหลักก่อน:<br><br>1. **Open Detail ยังไม่มี route รองรับ**<br><br>ใน `StaffTicketQueue.tsx` ตอนกด Open Detail จะไปที่ `/staff/tickets/:id` แต่ใน `App.tsx` ตอนนี้ยังไม่มี route นี้ มีแค่ `/tickets/:id` ของ Requester ดังนั้นกด Open Detail จาก Queue แล้วจะเข้า route ที่ไม่มี อยากให้เพิ่ม Staff Ticket Detail route ที่ถูกต้อง หรือถ้า Detail จะทำใน Issue ถัดไป อย่างน้อยต้องกำหนด route/placeholder ที่กดแล้วไม่ 404 เพราะ Definition of Done ของ Issue 5 ระบุว่าสามารถเปิดดูรายการตั๋วในคิวได้<br><br>2. **UI ยังไม่มี control สำหรับ Sorting**<br><br>ใน component มี `sortBy` และ `sortOrder` และ API รองรับ sorting แล้ว แต่หน้า UI ยังไม่มีให้ user เลือกว่าจะ sort ตามอะไร / ASC หรือ DESC ตอนนี้ใช้ default `createdAt desc` อย่างเดียว อยากให้เพิ่ม sort dropdown หรือ control บนหน้า Queue เพราะ Definition of Done ระบุว่าต้องสามารถ “จัดลำดับ” รายการตั๋วได้<br><br>มีอีก 2 จุดที่อยากให้เก็บถ้าไหว:<br>- Loading ตอนนี้เป็น spinner ยังไม่ใช่ skeleton ตาม scope ที่ระบุไว้<br>- เพิ่ม API test สำหรับ requestedPriority / itPriority / specific ownerId และ invalid pagination จะทำให้ coverage ของ filter ครบขึ้น<br><br>นอกนั้นเราเช็กแล้วโอเค ทั้ง role authorization, search/filter, pagination, requester 403, responsive mobile/desktop และ basic UI states | เราอัปเดตตามที่แนะนำครบทั้ง 4 จุดแล้ว:<br><br>1. เพิ่ม Route /staff/tickets/:id พร้อม StaffTicketDetailPlaceholder ใน App.tsx มีปุ่ม Back to Queue ป้องกันปัญหา 404 เรียบร้อย<br><br>2. เพิ่ม Dropdown ควบคุมการเรียงลำดับ (Sorting) บน UI รองรับ Newest/Oldest, Priority High-Low และ Status พร้อมผูกเข้ากับ query params โดยตรง<br><br>3. เปลี่ยน Loading state เป็น Skeleton rows ด้วย .placeholder-glow ตาม Zen Green ทั้ง Desktop table และ Mobile cards<br><br>4. เพิ่ม Fallback ให้ Pagination เมื่อได้ค่าลบ/invalid พร้อมเพิ่ม Assertion ใน staff-queue.api.test.ts สำหรับ itPriority, ownerId เฉพาะรายบุคคล และ Fallback pagination ครบถ้วน<br><br>รันเทสต์ผ่านครบ 100% ทั้ง Server และ Client ฝากตรวจหน่อยย | https://github.com/Piink7878/cpe334-TokTikIT/pull/56 |
| Feature/6 IT Staff Ticket Operations & Status Transitions #57 | Kamonnatt23 | Request changes | เราเช็ก PR #57 / commit `b2ad24e` ทั้ง backend, frontend, migration และ tests แล้ว โดยรวมฟีเจอร์หลักมาครบมาก <br><br>แต่ก่อน Approve ขอให้แก้อีก 2 จุดหลัก:<br><br>1. **เพิ่ม Server-side authorization tests สำหรับ Requester**<br><br>Backend ใส่ `requireRole(["IT_STAFF", "ADMIN"])` ให้ Claim / Assign / Priority / Status แล้ว ถือว่าตัว implementation ถูกทาง แต่ใน test ตอนนี้ Requester มี test แค่ GET Ticket Detail → 403 อยากให้เพิ่ม test ให้ Requester ยิง mutation ทุกตัวแล้วโดน 403 ด้วย: PATCH `/api/staff/tickets/:id/claim`, PATCH `/api/staff/tickets/:id/assign`, PATCH `/api/staff/tickets/:id/priority`, PATCH `/api/staff/tickets/:id/status` เพราะ Issue 6 ระบุว่าต้องป้องกัน Requester จากการแก้ไขเหล่านี้ด้วย Server-side authorization และใน Spec ก็ระบุชัดว่า UI hiding ไม่ถือเป็น security control<br><br>2. **AC-06 ระบุว่าการเปลี่ยน Status ต้อง log action**<br><br>ตอนนี้ `/status` update `currentStatus` และกรณี REJECTED มี Internal Note พร้อมเหตุผลแล้ว แต่ transition ปกติยังไม่มีการบันทึกว่าใครเปลี่ยน status จากอะไร → เป็นอะไร และเมื่อไหร่ อยากให้เช็กกับ Spec ว่าต้องมี audit/action log ตาม AC-06 หรือไม่ ถ้าต้องมี แนะนำเพิ่ม implementation + test ให้ครบ<br><br>อีกจุดที่อยากให้เก็บเรื่องเอกสาร:<br>PR นี้เพิ่ม `REJECTED` เข้า Status แต่ใน `docs/lab-03/specification.md` ตอนนี้ยังไม่มี `REJECTED` ใน status list ถ้าตั้งใจเพิ่ม status นี้จริง อยากให้อัปเดต Spec/API/Test Plan ให้ตรงกับ implementation ด้วย<br><br>นอกนั้นเราเช็กแล้วโอเค ทั้ง claim, assign, priority validation, transition matrix, terminal states และ rejection reason<br><br>แก้ 2 จุดหลักด้านบนแล้วเราโอเคให้ตรวจรอบสุดท้ายอีกที | ขอบคุณมากสำหรับคำแนะนำเรื่อง Security Tests, Action Logging และการซิงก์ Spec! ตอนนี้อัปเดตครบทั้ง 3 จุดเรียบร้อยแล้ว:<br><br>เพิ่มเคสทดสอบ 403 Forbidden ให้ครอบคลุมทุก Mutation Endpoints (claim, assign, priority, status) เมื่อถูกเรียกโดย REQUESTER ใน staff-operations.api.test.ts เรียบร้อย<br><br>เพิ่มการบันทึก Action/Audit Log สำหรับทุก Status Transition ตามข้อกำหนด AC-06 บันทึกว่าใครเป็นคนเปลี่ยน จากสถานะใดไปสถานะใด พร้อม Timestamp และเพิ่ม Integration Test ตรวจสอบ Log เรียบร้อย<br><br>อัปเดตเอกสาร docs/lab-03/specification.md และเอกสารที่เกี่ยวข้องให้ระบุสถานะ REJECTED ใน Status list พร้อมเงื่อนไข Rejection Note ให้ตรงกับ Implementation เรียบร้อย<br><br>รันเทสต์ผ่าน 100% ฝากตรวจอีกทีเน้อ | https://github.com/Piink7878/cpe334-TokTikIT/pull/57 |
| Feature/7 Admin User Management (Account Lifecycle & Password Resets) #58 | Kamonnatt23 | Request changes | เราไล่ตรวจ PR #58 / commit `b7564bef` เทียบกับ Scope, DoD, specification และ api-spec แล้ว ตอนนี้ยังมีหลายจุดที่เป็น blocker เพราะ core functionality ของ Issue 7 ยังไม่ครบ<br><br>🔴 1. ยังไม่มี Public Comment API: ใน api-spec ระบุว่าต้องมี GET/POST `/api/tickets/:id/comments`<br>🔴 2. ยังไม่มี Internal Note API: ต้องมี GET/POST `/api/tickets/:id/internal-notes` โดย Requester ต้องโดน 403 ทั้งการอ่านและการสร้าง<br>🔴 3. Staff Ticket Detail ยังโพสต์ Comment/Note ไม่ได้<br>🔴 4. Requester ยังไม่มีปุ่ม Problem Appears Resolved (BR-05)<br>🔴 5. GET `/api/tickets/:id` ของ Requester ยังไม่มี Public Comments<br>🟡 6. Documentation กับ data model ไม่ตรงกัน<br>🔴 7. Tests ยังต้องเพิ่มตาม behavior จริง<br><br>จุดหลักที่ต้องทำก่อนคือ Public Comment API, Internal Note API, UI สำหรับโพสต์ทั้งสองประเภท และ Problem Appears Resolved | ขอบคุณมากสำหรับคำแนะนำเรื่อง Scope ที่ถูกต้องของ Issue 7! เราแก้ Blocker ครบทุกจุดตาม Spec และ Definition of Done เรียบร้อยแล้ว ทั้ง Public Comments API, Internal Notes API, Form Control โพสต์สองประเภทใน Staff Ticket Detail, ปุ่ม Problem Appears Resolved (BR-05), Zero Information Leakage, และเพิ่ม Test ครบถ้วน 100% (Server 104 tests, Client ครบถ้วน รวม 151 tests) ฝากตรวจอีกรอบหน่อยเน้อ | https://github.com/Piink7878/cpe334-TokTikIT/pull/58 |
| Feature/7-ticket-comments-and-notes #59 | Kamonnatt23 | Request changes | เราเช็ก PR #59 แล้ว เหลือ 2 จุดหลักที่อยากให้แก้ก่อน Approve<br><br>🔴 `POST /indicate-resolved` ยังไม่ได้จำกัด Role เป็น `REQUESTER` ทำให้ IT Staff/Admin เรียก endpoint นี้ได้ด้วย แนะนำเพิ่ม `requireRole(["REQUESTER"])` และ test ให้ครบ<br><br>🔴 ตอน `indicate-resolved` ระบบสร้าง `InternalNote` โดยใช้ Requester เป็น `authorId` ซึ่งขัดกับ requirement ที่ Requester ห้ามสร้าง Internal Note แนะนำให้ action นี้สร้างแค่ Public Comment หรือแยกเป็น system event<br><br>🟡 เพิ่ม test ยืนยันว่าการกด Problem Appears Resolved แล้ว Ticket status ต้องไม่เปลี่ยนเป็น `RESOLVED/CLOSED`<br><br>นอกนั้นเราเช็กแล้วโอเค ทั้ง Comment, Internal Note authorization, append-only และ UI<br><br>แก้ 2 จุดแรก + เพิ่ม test status แล้วน่าจะผ่านได้เลย | ขอบใจที่ช่วยรีวิว เราแก้โค้ดและ push อัปเดตไปแล้วตาม 3 จุดที่บอกเลย<br><br>🔴 ใส่ requireRole(["REQUESTER"]) ในเส้น /indicate-resolved เรียบร้อย<br>🔴 เอาการสร้าง Internal Note ออก เปลี่ยนเป็นสร้างแค่ Public Comment เพื่อแจ้งให้ IT Staff รู้แทน<br>🟡 เพิ่มเทสยืนยันว่าสเตตัส Ticket จะไม่ถูกเปลี่ยนเป็น RESOLVED/CLOSED แน่นอน และเพิ่มเคสเทส 403 กรณี IT Staff/Admin เผลอเรียกเส้นนี้ด้วย<br><br>ตอนนี้ฝั่ง Server รันเทสผ่าน 100% หมดแล้ว ฝากรบกวนเช็กอีกรอบนะ | https://github.com/Piink7878/cpe334-TokTikIT/pull/59 |
| Feature/8-admin-user-management #60 | Kamonnatt23 | Request changes | เราเช็ก PR #60 แล้ว เจอ 3 จุดที่อยากให้เก็บเพิ่ม<br><br>1. API test ยังขาด Edit + Reset Password + List/Search/Filter และ safety case ของ last Admin ตอน deactivate<br><br>2. Edit User ถ้าส่ง `role` ที่ไม่ถูกต้อง ตอนนี้จะตอบ 200 แต่ไม่เปลี่ยน role เพราะ `validRoles.includes()` แค่ข้ามการ update แนะนำให้ invalid role → 400 เหมือน Create<br><br>3. UI test ตอนนี้เช็กแค่ render/search/filter/list ยังไม่ได้ test Create, Edit, Reset Password interaction<br><br>ข้อ 1-2 อยากให้แก้ก่อน ส่วนข้อ 3 เพิ่มเพื่อให้ Test Coverage ตรงกับ Scope<br><br>นอกนั้น Authorization + Safety Rules หลัก ๆ โอเคแล้ว | แต๊งกิ้วที่ช่วยดูให้ เราแก้โค้ดและ push อัปเดตไปเรียบร้อยละนะ<br><br>API test: เติมเทสต์ Edit, Reset Password, List/Search/Filter ครบละ แล้วก็เพิ่มเคส safety ตอน deactivate แอดมินคนสุดท้ายเรียบร้อย<br><br>Edit User: ปรับให้โยน 400 Bad Request เวลารับค่า role มั่วๆ มาแล้ว (เหมือนของ Create เลย)<br><br>UI test: เพิ่ม test case ตอนกด Create, Edit, แล้วก็ Reset Password ให้ครบตาม interaction ละ<br><br>รันเทสต์ใหม่ผ่านหมด 100% ละ ฝากดูอีกรอบนะ ถ้าโอเคฝากกด Approve ที | https://github.com/Piink7878/cpe334-TokTikIT/pull/60 |
| Feature/9-e2e-and-security #61 | Kamonnatt23 | Request changes | เราไล่ commit `2abe48c` เพิ่มแล้ว เจอจุดที่อยากให้แก้ก่อน Approve:<br><br>**1. Staff E2E มี URL regex ผิด**<br>ตอนเช็ก redirect เป็น `/s t a f f-q u e u e/` มี space แทรก ต้องเป็น `/staff-queue`<br><br>**2. ยังไม่มี E2E ยืนยันว่า Requester มองไม่เห็น Internal Note**<br>ตอนนี้ทดสอบแค่ Staff สร้างและเห็น Internal Note แต่ security requirement สำคัญคือ note ต้องไม่รั่วไป Requester ควรเพิ่ม flow นี้<br><br>**3. E2E ยัง verify แค่ UI ไม่ได้ verify DB state หลัง workflow**<br>Claim / Priority / Status / Comment / Internal Note ควรมี assertion ว่า state ถูกบันทึกจริง ไม่ใช่แค่เห็นข้อความ success<br><br>**4. `tests.md` ยังมี MIG-01 และ UI-06 เป็น Planned**<br>แต่ Issue 9 ระบุว่าชุดทดสอบทั้งหมดผ่าน 100% ถ้า 2 ตัวนี้อยู่นอก scope ให้แก้ wording เป็น “all tests in Issue 9 scope passed” ไม่งั้นเอกสารขัดกัน<br><br>**5. E2E พึ่ง seed data + hard-coded account**<br>`staff2@toktikit.local / Password123!` และ Category/RelatedSystem ต้องมีอยู่ก่อน ทำให้ test ไม่ self-contained<br><br>จุด 1 กับ 2 เราว่าเป็น blocker ก่อน Approve ส่วน 3-5 แนะนำเก็บให้ครบ | เราแก้ตามคอมเมนต์เรียบร้อยละ!<br><br>- ลบ space ตรง regex /staff-queue ทิ้งแล้ว<br>- เพิ่ม flow ทดสอบสิทธิ์ละ หลังจาก Staff พิมพ์ Note เสร็จ จะสั่ง logout แล้วล็อกอินเป็น Requester เข้าไปเช็กหน้าตั๋วใบเดิม แล้วใช้ Playwright assert ยืนยันเลยว่า DOM ไม่ render Internal Note แน่นอน (เทสต์ BR-04)<br>- เรื่อง State persistence เราไม่ต่อ DB ตรง ๆ แต่ใช้วิธีสั่ง page.reload() หลังทำ action ฝั่ง Staff แล้วเช็ก UI ซ้ำว่าสถานะเปลี่ยนจริงและอยู่ครบ<br>- tests.md ปรับสถานะ MIG-01 กับ UI-06 เป็น Pass หมดแล้ว<br>- แก้ E2E ไม่ให้พึ่ง Seed data ละ เปลี่ยนไปยิง API จำลองฝั่ง Requester สร้างตั๋วใหม่สด ๆ ในบล็อก beforeAll ก่อนเริ่มเทสต์แทน<br>- ตอนนี้รันเทสต์ผ่าน 100% ทั้ง Server (142 tests), Client (50 tests) และ E2E (18 tests) เขียวทั้งโปรเจกต์ละ ฝากรบกวนตรวจหน่อยน้า | https://github.com/Piink7878/cpe334-TokTikIT/pull/61 |

## Pull Requests I reviewed for my partner

| Partner PR | Decision | My Comment | Partner Response | Evidence |
|------------|----------|------------|------------------|----------|
| docs: add Lab 3 engineering specification and test plan #43 | Request changes | มีที่อยากให้แก้ประมาณนี้<br><br>1. Test plan ยัง trace AC-01 ถึง AC-16 ไม่ครบ โดยเฉพาะ AC-08 (assign ต้องเป็น active IT Staff), AC-09 (condition ตอนเปลี่ยน status), AC-10 (Resolved/Closed → Reopened), AC-11 (Internal Note immutability) และ AC-15 (last admin ทั้ง deactivate + role change) อยากให้เพิ่ม test case ให้ครบตาม AC<br><br>2. เรื่อง Internal Notes อยากให้แยก test permission ชัด ๆ ว่า Admin GET ได้ 200 แต่ POST ต้อง 403 ส่วน Requester ต้องอ่าน/เขียนไม่ได้ เพราะตอนนี้ AUTHZ-02 ยังรวมหลายกรณีไว้ด้วยกัน<br><br>3. `Appears Resolved` ใน UI บอกว่าใช้ได้เฉพาะตอน In Progress แต่ API/spec ยังไม่ได้กำหนด condition นี้ชัดเจน แนะนำเขียนเป็น business rule ไปเลย และเพิ่ม test กรณีเรียกตอน status อื่น<br><br>4. Status transition เรื่อง Resolved/Closed → Reopened ระบุว่าต้องเป็น comment ที่ "indicating issue persists" แต่ยังไม่บอกว่าจะตรวจยังไง ควรกำหนด behavior ให้ชัดก่อน implement<br><br>5. Attachment API ตอนนี้มีแค่ list endpoint แต่ยังไม่มี request/response, permission และ status code ราย endpoint แบบละเอียด อยากให้เพิ่ม เพราะ Issue 5 ต้องเอา authorization ไปใช้กับส่วนนี้<br><br>6. มีบางจุดใน spec ที่ยังไม่ตรงกัน เช่น bcrypt vs bcrypt/Argon2 และ session ID vs token hash ควรเลือก behavior เดียวให้เป็น contract เดียวกัน<br><br>7. `VIS-01` map กับ AC-16 แต่ AC-16 เป็น migration/regression ไม่ใช่ responsive UI ตรงนี้น่าจะ map ผิด | แก้ตามคอมเมนต์แล้วนะ<br><br>- เพิ่ม test ให้ trace AC ครบทุกข้อ โดยเฉพาะเรื่อง active IT Staff, status transition, Reopened และ last Admin<br>- แยก test permission ของ Internal Notes ระหว่าง Requester กับ Admin ให้ชัดขึ้น<br>- กำหนด Appears Resolved ให้ใช้ได้เฉพาะตอน In Progress และเพิ่ม test ตอนเรียกผิดสถานะ<br>- ทำ behavior ของ Resolved/Closed → Reopened ให้ชัดเจนขึ้น<br>- เพิ่มรายละเอียด Attachment API ครบทุก endpoint ทั้ง request/response, permission และ error status<br>- ทำ auth/session กับ password hashing ให้ใช้ design เดียวกันทุกไฟล์<br>- แก้ mapping ของ VIS-01 ไปเป็น AC ที่เกี่ยวกับ responsive โดยตรง และแยก AC-16 ไว้สำหรับ migration/regression<br>- แล้วก็แก้ wording เรื่อง Admin จาก CRUD เป็น List/Create/Edit/Activate-Deactivate/Reset Password เพื่อไม่ให้สื่อว่าลบ user ได้ | https://github.com/Kamonnatt23/toktickit/pull/43 |
| feat: add Lab 3 user database migration and seed #44 | Request changes | มีบางจุดที่อยากให้แก้/clarify<br><br>1. Issue 2 ระบุว่าต้อง migrate โดยไม่ให้ข้อมูล Ticket หรือ Attachment สูญหาย ตอนนี้ `database.test.ts` ตรวจ existing Ticket แล้ว แต่ยังไม่มี test ที่ตรวจ Existing Attachment ว่ายังอยู่ และ `ticketId`/metadata ยังสัมพันธ์กับ Ticket เดิมหลัง migration แนะนำเพิ่ม test ส่วนนี้ครับ อันนี้คิดว่าต้องมี<br><br>2. ใน Prisma เปลี่ยน model เป็น `User` แต่ใช้ `@@map("RequesterUser")` ทำให้ physical table ใน DB ยังชื่อ `RequesterUser` อยู่ ถ้าตั้งใจ reuse table เดิมเพื่อ preserve Lab 2 data ก็โอเคครับ แต่อยากให้ระบุ/confirm design นี้ให้ชัด เพราะ Scope บอกว่า migrate Requester เดิมเข้ากับ User ใหม่<br><br>3. Scope ของ Issue ระบุ User มี `Status` แต่ schema ใช้ `isActive` แนะนำให้เช็กกับ contract ว่าต้องการ `status = Active/Inactive` หรือ `isActive` กันแน่ เพราะ Issue 9 จะเอา status/deactivate ไปใช้ต่อครับ<br><br>4. Migration ตั้ง existing users ให้ `passwordHash = ''` ส่วน seed ค่อยใส่ bcrypt hash ให้ user ที่อยู่ใน seed ถ้ามี existing user ที่ไม่ได้อยู่ใน seed จะ login ไม่ได้ แนะนำระบุ migration strategy/default credential ให้ชัดว่า existing users จะถูกจัดการยังไงครับ | แก้ตาม comment แล้วจ้า<br><br>- เพิ่ม test ตรวจ Existing Lab 2 Attachment ว่ายังอยู่หลัง migration พร้อมเช็ก ticketId, metadata และความสัมพันธ์กับ Ticket เดิม<br>- เพิ่ม clarification ว่า Prisma ใช้ model User แต่ map กับ physical table เดิม RequesterUser ผ่าน @@map() เพื่อ preserve ข้อมูล Lab 2<br>- ปรับ terminology เรื่อง account status ให้ใช้ isActive ให้ตรงกันทั้ง contract และ schema<br>- แก้ migration strategy เรื่อง initial password ของ legacy users ทุกคน ให้มี bcrypt hash + requiresPasswordChange = true แม้ user จะไม่ได้อยู่ใน seed<br>- รัน database tests และตอนนี้ ผ่าน 7/7 tests แล้ว<br>- ยืนยันว่าไม่มี authentication/API/UI ของ Issue 3 ถูกเพิ่มเข้ามา | https://github.com/Kamonnatt23/toktickit/pull/44 |
| feat: add Lab 3 authentication and session management #45 | Request changes | มีจุดที่อยากให้แก้เพิ่มก่อน คือ test change-password มีการเปลี่ยน password และ requiresPasswordChange ของ seed user แต่หลัง test จบยังไม่ได้ restore DB state กลับ ทำให้ test อื่นที่รันต่ออาจเจอข้อมูลไม่เหมือน seed เดิม แนะนำเพิ่ม cleanup/restore ใน afterAll() รวมถึง cleanup session ที่ test สร้างไว้<br><br>แก้ test isolation จุดนี้แล้ว สำหรับเรา Approve ได้เลย | แก้ไขเรียบร้อยค่ะ งับ ครับผม | https://github.com/Kamonnatt23/toktickit/pull/45 |
| feat: add authentication UI and session integration #46 | Request changes | มี 2 จุดที่อยากให้แก้ก่อน<br><br>- Attachment authorization: ใน attachments.ts ตอนนี้เปลี่ยนมาใช้ requireAuth แต่ยังเช็กแค่ ticket.requesterId === currentUser.id ทำให้ IT Staff ไม่สามารถจัดการ attachment ของ ticket อื่นได้ ทั้งที่ contract ของ Issue 1 ระบุว่า IT Staff จัดการ Attachment ได้ และ Admin ต้องถูก deny แนะนำให้คง authorization ตาม role contract เดิมไว้<br>- Error response: ตอนนี้ Attachment API ส่ง err.message กลับไปให้ client ใน 500 response ซึ่งอาจ expose internal error detail แนะนำให้ log ไว้ฝั่ง server แต่ response ให้เป็น safe generic error ตาม contract<br><br>ส่วนที่เหลือของ Issue 4 โดยรวมโอเคและ | แก้ตาม comment แล้วนะ<br><br>แก้ Attachment authorization ใน attachments.ts ให้ใช้ session identity + role ตาม contract:<br>Requester จัดการได้เฉพาะ ticket ของตัวเอง<br>IT Staff จัดการ attachment ได้เฉพาะ ticket ที่ยังไม่ assigned หรือ assigned ให้ตัวเอง<br>Admin ดู/download ได้แบบ read-only แต่ upload/delete ไม่ได้<br>แก้ 500 response ไม่ให้ส่ง err.message ออกไปแล้ว ตอนนี้ log error ฝั่ง server และคืน { error: "Internal server error" }<br>เพิ่ม/ปรับ attachment API tests ครบตามกรณี authorization และ safe error response<br><br>ตอนนี้ backend tests ผ่าน 58/58 และ frontend tests ผ่าน 31/31 แล้ว | https://github.com/Kamonnatt23/toktickit/pull/46 |
| Issue #5: Role-Based Authorization & Requester Regression #47 | Request changes | ตรวจ commit Issue 5 แล้วค่ะ 👍 แนวทางเรื่องใช้ session identity และเพิ่ม test IT Staff/Admin ถูกทางแล้ว แต่มี 3 จุดที่ต้องแก้ให้ตรง contract ค่ะ<br><br>1. `GET /api/tickets` ตอนนี้เปิดให้ IT Staff/Admin เห็นทุก ticket แต่ endpoint นี้คือ **View My Tickets** ซึ่งตาม Authorization Matrix ให้ Requester เท่านั้นค่ะ ส่วน Staff/Admin ต้องใช้ `/api/staff/tickets` สำหรับ queue<br><br>2. `POST /api/tickets` ตอนนี้ block แค่ Administrator ทำให้ IT Staff ยังสร้าง ticket ได้ แต่ contract กำหนด **Create Ticket = Requester only** ค่ะ แนะนำเช็ก `user.role !== 'Requester'` แล้ว return `403`<br><br>3. Ticket ของ Requester คนอื่น ตอนนี้ implementation และ test ยังใช้ `403` แต่ Error Policy กำหนด resource ที่ ownership-protected ให้ return `404` เพื่อป้องกัน enumeration ค่ะ ดังนั้น test `returns 403...someone elses ticket` ควรเปลี่ยนเป็น `404` และ implementation ให้ return `404` ด้วย<br><br>ส่วนการเปลี่ยนมาใช้ `req.user` แทน `X-Requester-Id` และ test Staff/Admin ดู ticket ได้ ถือว่าตรง direction ของ Issue 5 แล้วค่ะ | แก้ตาม 3 comment แล้วนะ<br><br>1. `GET /api/tickets` ปรับเป็น **Requester-only** แล้ว<br>   - Requester → 200 และเห็นเฉพาะ ticket ของตัวเอง<br>   - IT Staff / Administrator → 403<br>   - Staff Queue จะใช้ endpoint แยกใน Issue #6<br><br>2. `POST /api/tickets` ปรับเป็น **Requester-only** แล้ว<br>   - Requester → 201<br>   - IT Staff / Administrator → 403<br><br>3. แก้การเข้าถึง Ticket ของ Requester คนอื่น จาก `403` เป็น **404** ทั้ง implementation และ test เพื่อให้ตรงกับ non-enumeration policy<br><br>เพิ่ม test การ spoof `X-Requester-Id` ด้วย และ backend ยังคงใช้ `req.user` จาก session เป็นตัวระบุ identity | https://github.com/Kamonnatt23/toktickit/pull/47 |
| feat: implement IT staff ticket queue #48 | Request changes | มี 3 จุดที่อยากให้แก้ก่อน<br><br>Test cleanup<br>staff-queue.api.test.ts import afterAll มาแล้ว แต่ยังไม่ได้ cleanup ข้อมูลที่สร้างใน test ทั้ง User / Session / Category / RelatedSystem / Ticket ทำให้ DB อาจมี test data ค้างและกระทบ test อื่นได้<br><br>Assigned To filter<br>ตอนนี้ UI เลือกได้แค่ Unassigned กับ Assigned to Me แต่ใน UI spec ระบุ Filter เป็น Assigned To และควรเลือก assignee คนอื่นได้ด้วยค่ะ แนะนำโหลดรายชื่อ active IT Staff มาเป็น options ใน dropdown<br><br>Pagination<br>ตอนนี้มีแค่ Previous / Next / Items per page แต่ UI spec ระบุว่าต้องมี Page numbers ด้วย เช่น 1 2 3 ... เพื่อให้กดข้ามหน้าได้<br><br>ส่วน API /api/staff/tickets เช็กแล้ว role, search, filter, sort, pagination และ invalid query handling ตรงกับ contract<br><br>อีกจุดที่แนะนำเพิ่มคือ test sorting ตอนนี้เช็กแค่ว่า response กลับมา 200 แต่ยังไม่ได้ verify ว่าข้อมูลเรียงตาม sortBy/sortOrder จริงค่ะ | แก้ตาม comment แล้วนะครับ<br><br>เพิ่ม afterAll() cleanup สำหรับ User, Session, Ticket, Category และ RelatedSystem ที่สร้างใน test<br>เพิ่ม Assigned To filter และปรับ UI ให้รองรับ assignee ตาม contract โดยไม่ได้ใช้ /api/dev/users แล้ว<br>เพิ่ม pagination แบบมี page numbers พร้อม Previous/Next<br>เพิ่ม test sorting โดย verify ลำดับข้อมูลจริงตาม sortBy / sortOrder<br>เพิ่ม test สำหรับ Assigned To / pagination และ regression ที่เกี่ยวข้อง | https://github.com/Kamonnatt23/toktickit/pull/48 |
| feat: implement IT staff ticket operations and detail #49 | Request changes | ตรวจ Issue 7 commit นี้แล้ว มี 2 จุดที่ต้องแก้ก่อน<br><br>Admin ยังเข้า Staff Ticket Detail ไม่ได้<br>ใน App.tsx ตอนนี้ StaffTicketDetail render เฉพาะ user.role === 'IT Staff' ทำให้ Admin ถึงจะมี UI read-only ใน component แล้ว ก็ไม่มีทางเปิด detail จาก Queue ได้<br><br>แต่ใน ui-spec.md ระบุว่า Staff Ticket Detail access ได้ทั้ง IT Staff และ Administrator โดย Admin เป็น read-only<br><br>ดังนั้น route ควรให้ Admin เข้า detail ได้ด้วย และใน StaffQueue ตอนนี้ก็ block Admin จากการ click row อยู่ ควรแก้ให้ Admin click เข้า detail ได้ แต่ controls ใน detail ยัง hidden/disabled เหมือนที่ทำไว้แล้ว<br><br>หลัง Assign / Status / IT Priority ข้อมูล ticket ใน frontend จะไม่ครบ<br>API PATCH /assign และ PATCH /status return แค่ ticket record ที่ update ไม่ได้ include requester, category, relatedSystem, attachments, owner<br><br>แต่ frontend เอา response ไป setTicket(data) แล้ว render ticket.requester.name, ticket.category.name ฯลฯ ต่อ ทำให้หลัง Claim/Assign หรือ update status/priority มีโอกาสหน้า detail crash เพราะ relations หาย<br><br>แนะนำให้ refetch fetchTicket() หลัง mutation หรือแก้ API ให้ return ticket พร้อม relations<br><br>อีกจุดที่อยากให้เพิ่ม test คือ In Progress -> Resolved ทั้งกรณีไม่มี comment ต้อง 400 และมี comment ต้อง 200 พร้อมเช็กว่า PublicComment ถูกสร้างจริงและ author เป็น IT Staff | ขอบคุณสำหรับรีวิว เราแก้ประเด็นที่แจ้งมาแล้ว<br><br>1. Administrator Read-only Access<br>- Admin เข้า Staff Queue และ Staff Ticket Detail ได้แล้ว<br>- Admin ดูข้อมูล Ticket ได้อย่างเดียว<br>- Admin ไม่สามารถ Claim/Assign/Reassign, เปลี่ยน IT Priority หรือเปลี่ยน Status ได้<br>- Backend ของ Staff mutation ยังอนุญาตเฉพาะ IT Staff เท่านั้น<br><br>2. PATCH Response / Relation Loss<br>- เอา setTicket(data) ออกจากหลัง mutation<br>- หลัง Assign, IT Priority และ Status สำเร็จ จะเรียก fetchTicket() เพื่อโหลดข้อมูล Ticket ใหม่ทั้งหมด<br>- ทำให้ข้อมูลที่เกี่ยวข้อง เช่น requester, category, system, owner และ attachments ยังอยู่ครบ<br><br>3. In Progress → Resolved Tests<br>- ไม่มี comment → 400, status ไม่เปลี่ยน และไม่มี PublicComment<br>- มี comment → 200, status เปลี่ยนเป็น Resolved และสร้าง PublicComment โดย author เป็น IT Staff ที่ authenticated<br><br>Verification:<br>- Backend: 99/99 passed<br>- Frontend: 47/47 passed<br>- ไม่มีไฟล์ที่ไม่เกี่ยวข้องหรือ generated files ถูกเพิ่มมา | https://github.com/Kamonnatt23/toktickit/pull/49 |
| feat: implement public comments and internal notes #50 | Request changes | ตรวจ commit นี้แล้ว เจอ 1 จุดที่ต้องแก้ก่อน<br><br>🔴Admin โพสต์ Public Comment ไม่ได้<br><br>ใน scope ระบุว่า Public Comments ใช้สื่อสารระหว่าง Requester, IT Staff และ Admin แต่ตอนนี้ backend block Admin ด้วย 403 และ frontend ก็ซ่อน form สำหรับ Admin ทำให้ Admin อ่านได้อย่างเดียว<br><br>ควรแก้ให้ Admin สามารถ POST Public Comment ได้ แล้วเพิ่ม test กรณี Admin post comment สำเร็จด้วย<br><br>อีกจุดเป็น test quality:<br>ตอนนี้ COMM-01/02 ลอง PATCH/DELETE URL ที่ไม่มี route แล้วเช็ก 404 ซึ่งยังไม่ได้พิสูจน์ว่า comment/note ที่มีอยู่จริงถูกห้ามแก้หรือลบ ถ้าเพิ่ม test โดยสร้าง record จริงแล้วลอง mutate จะครอบคลุม append-only ชัดกว่า<br><br>นอกนั้น flow หลักของ Public Comment / Internal Note ที่ตรวจตรงกับ scope แล้ว | อัปเดต Issue #8 เพิ่มเติมนะ เรื่องที่ reviewer ทักว่า Admin ควรโพสต์ Public Comment ได้ เราเช็กกับ specification ใน repo แล้ว เลยไม่ได้แก้ส่วนนี้<br><br>เหตุผลคือ requirement ที่อนุมัติใน repo กำหนดให้ Administrator เข้าดู Ticket, Public Comments และ Internal Notes ได้แบบ read-only และไม่ได้ให้สิทธิ์ Admin โพสต์ Comment หรือ Note เอง ส่วนสิทธิ์ POST Public Comment ตอนนี้กำหนดไว้สำหรับ Requester (เฉพาะ ticket ของตัวเอง) และ IT Staff<br><br>ดังนั้นตอนนี้ Admin เลยยังเป็น 403 ตอนพยายามโพสต์ และหน้า UI ก็ไม่ได้แสดงช่องโพสต์ให้ Admin ซึ่งตรงกับ authorization matrix ที่มีอยู่<br><br>ส่วนที่เราแก้เพิ่มในรอบนี้คือ<br>- แก้ TypeScript error ใน `server/src/app.ts`<br>- ปรับ append-only tests ให้สร้าง Comment/Note จริง แล้วลอง PATCH/DELETE กับ record จริง พร้อมเช็ก DB ว่าข้อมูลไม่ถูกแก้หรือลบ<br>- Backend 110/110 ผ่าน<br>- Frontend 50/50 ผ่าน<br><br>เลยคง Admin authorization เดิมไว้ตาม spec | https://github.com/Kamonnatt23/toktickit/pull/50 |
| feat: implement Issue 9 admin user management #51 | Request changes | ตรวจ Issue 9 commit นี้แล้ว เจอ 1 จุดที่ต้องแก้ก่อน<br><br>🔴 ADMIN-03 / ADMIN-04 ใน tests.md ถูกเปลี่ยนเป็น Pass แล้ว แต่ใน users-admin.api.test.ts ยังไม่มี test ของ 2 case นี้จริง<br><br>ตอนนี้มี test duplicate email, self-deactivation, create/edit/reset password แล้ว แต่ยังไม่มี:<br><br>* Last active Admin deactivation → ต้องได้ 400 และ DB ต้องยัง active<br>* Last active Admin role change → ต้องได้ 400 และ DB ต้องยังเป็น Administrator<br><br>ตัว implementation มี guard สำหรับ 2 case นี้แล้ว ดังนั้นไม่ได้บอกว่า production logic ผิด แต่ test ยังไม่ได้พิสูจน์ behavior ตาม AC-15 และไม่ควร mark Pass จนกว่าจะมี test จริง<br><br>อีกจุดที่อยากให้แก้ด้วยคือ afterAll ตอนนี้ยังว่างอยู่ ถึงจะใช้ unique email แต่ test สร้าง user/session หลายตัวแล้วไม่ได้ cleanup ทำให้ DB สะสมข้อมูลทุกครั้งที่รัน | อัปเดตจาก comment ที่ทักมา เราแก้ตามนี้แล้ว<br><br>- เพิ่ม test `ADMIN-03` สำหรับกรณี Last Active Administrator Deactivation และเช็กต่อใน DB ว่า account ยัง `isActive: true`<br>- เพิ่ม test `ADMIN-04` สำหรับกรณี Last Active Administrator Role Change และเช็กว่า role ยังเป็น `Administrator`<br>- เพิ่ม `afterAll` cleanup ใน `users-admin.api.test.ts` ลบเฉพาะข้อมูล test ที่สร้างเอง เพื่อไม่ให้ DB สะสม<br>- แก้ race condition ที่เกิดจาก test หลายชุดใช้ email ซ้ำกัน โดยเพิ่ม random suffix ใน `my-tickets.api.test.ts`<br>- แก้ TypeScript error ใน `UserManagement.tsx` และ `UserManagement.test.tsx` ที่ใช้ `api` ซึ่งไม่มีใน architecture เดิม ให้ใช้ `fetch` + `vi.stubGlobal()` ตาม pattern ของ repo<br>- แก้ `global.fetch` ใน `App.test.tsx` เป็น `vi.stubGlobal('fetch', mockFetch)`<br>- แก้ test assertion ของ Admin `User Management` ให้เลือกจาก role ของปุ่ม เพราะหน้าใหม่มีข้อความ User Management ซ้ำทั้งใน nav และ heading<br>- ลบ trailing whitespace ใน `server/src/app.ts`<br>- ลบ temporary scripts ที่ไม่เกี่ยวกับโปรเจกต์ออกแล้ว<br><br>ผลล่าสุด:<br>Backend 120/120 ผ่าน<br>Frontend 53/53 ผ่าน<br>git diff --check ผ่าน<br><br>Production logic และ authorization ของ Issue 9 ไม่ได้ถูกเปลี่ยนจาก comment รอบนี้ | https://github.com/Kamonnatt23/toktickit/pull/51 |
| test: Testing, E2E, Responsive & Final Integration #52 | Request changes | ตรวจ commit 0441a34 เทียบกับ Scope Issue 10 แล้ว มีบางจุดที่ยังไม่ตรงกับ Test Plan เลยยังไม่ Approve รอบนี้<br><br>1. Responsive ยังขาด Tablet<br>   ตอนนี้ responsive.spec.ts มีแค่ Desktop 1200x800 กับ Mobile 400x800 แต่ Scope ระบุ Desktop + Tablet + Mobile<br><br>2. ยังไม่ได้ทำ Screenshot Evidence จริง<br>   test ชื่อ takes screenshots แต่ใน code ไม่มี page.screenshot() มีแค่ setViewportSize แล้ว assert element ดังนั้นตอนนี้ยังเป็น responsive E2E แต่ยังไม่มี screenshot evidence ตาม scope<br><br>3. VIS-01 ถูกลด scope ลง<br>   เดิม Test Plan ระบุ no horizontal overflow + accessibility + Desktop/Tablet/Mobile แต่รอบนี้ tests.md เปลี่ยนเหลือ Desktop/Mobile และเช็กแค่ element visible/usable ถ้า requirement เดิมยังใช้ ต้องเพิ่ม test แทนการลด scope<br><br>4. E2E-01 ยังไม่ได้ test URL hopping<br>   ใน tests.md ระบุ blocks URL hopping (404) แต่ authentication.spec.ts ยังไม่มีการลองเข้า protected URL โดยตรง<br><br>5. E2E-02 ยังไม่ตรงกับ description<br>   tests.md บอกว่า Search + Claim แต่ code ตอนนี้ทำ filter + sort และ assign ให้ Staff One ยังไม่ได้ test search กับ claim flow โดยตรง<br><br>6. มีเรื่อง test isolation ที่อยากให้ระวัง<br>   playwright.config.ts เปิด fullyParallel และหลาย spec ใช้ user เดียวกัน เช่น charlie/admin แล้วแก้ DB state จริง ถ้ารัน parallel มีโอกาส race กัน ถึงตอนนี้ 5/5 จะผ่านหลายรอบก็ตาม แนะนำแยก test user ต่อ spec หรือจัด isolation/serial ให้ชัดเจน<br><br>หลักๆ อยากให้แก้ 4 เรื่องแรกก่อน โดยเฉพาะ Tablet + screenshot evidence + URL hopping + Search/Claim เพราะเป็นเรื่อง coverage ที่ Test Plan ระบุไว้แล้ว<br><br>Production logic รอบนี้ไม่ได้มีปัญหา ประเด็นอยู่ที่ Final Test Coverage กับ Evidence เป็นหลัก | แก้ Issue #10 หลัก ๆ เป็นการทำ final test + release check นะ<br><br>เพิ่ม/ปรับ E2E tests สำหรับ Authentication, Staff Ticket Flow และ Admin User Management<br>เพิ่ม responsive test ครบ Desktop / Tablet / Mobile สำหรับ Requester, IT Staff, Admin<br>เพิ่ม screenshot evidence ครบ 9 รูปไว้ใน artifacts/lab-03/screenshots/<br>แก้ test locator/flow ให้ตรงกับ UI จริง เช่น Search, Claim Ticket, User Management และ Change Password<br>เพิ่ม test isolation เพื่อให้ E2E รัน parallel แล้วไม่ชนกัน<br>เพิ่ม @types/node สำหรับ Playwright test ที่ใช้ child_process<br>แก้ bug ใน StaffTicketDetail.tsx จาก /api/admin/users เป็น /api/staff/users เพราะ Staff เรียก Admin endpoint แล้วโดน 403<br>อัปเดต docs/lab-03/tests.md ให้ตรงกับ test/evidence จริง<br><br>Test สุดท้ายผ่าน E2E 6/6, Frontend 53/53, Backend 119/119 และ git diff --check ผ่าน | https://github.com/Kamonnatt23/toktickit/pull/52 |
