# Lab 1 — AI Use and Reflection  (fill this in)

**LLM/agent used:** <copilot>

## Selected key prompts (6–10)
| # | Prompt | Reflection |
|---|--------|------------|
| 1 | Read the enclosed TokTickIT Lab 1 requirements. Summarize the four GitHub Issues, their dependencies, required outputs, and required automated tests. Propose an implementation order, but do not write code yet. เป็นไทย แต่คำไหนเป็น keyword ให้คงไว้ | This worked in one shot. |
| 2 | Verify the TokTickIT project foundation setup across client/ and server/. Ensure that client/ builds correctly with React, TypeScript, and Bootstrap, and server/ runs Express with Prisma initialized. Verify that Vitest and Supertest configurations are intact. Check that root .gitignore excludes node_modules/, .env, dist/, and build/. Do not add any new feature code outside of the Issue 1 foundation scope. | Agent created the core files correctly. I had to manually adjust the script commands in package.json to ensure Vitest and Supertest ran seamlessly from the root folder. |
| 3 | Implement Issue 2: API Health Check for TokTickIT according to the Lab 1 specifications:Backend (server/): Add a GET /api/health endpoint in Express that returns HTTP 200 with JSON { "status": "ok", "service": "TokTickIT API" }. Make sure CORS is properly configured for the client origin.  Testing (server/tests/lab-01/health.test.ts): Write an automated test using Supertest (named test case API-01) to verify that GET /api/health responds with HTTP 200 and the expected JSON body.  Frontend (client/): In src/api.ts and src/App.tsx, create the function and UI state to call GET /api/health. When the backend responds successfully, display the backend status as 'Online'. If the backend is unavailable or fails, display an informative error message.  Do not implement the categories feature yet. | The agent created the endpoint and the Supertest case cleanly. I verified CORS settings between client (port 5173) and server (port 3000) so the fetch request works without browser blocks. |
| 4 | Implement Issue 3: Create and seed IT request categories for TokTickIT according to the Lab 1 specifications:Prisma Schema (server/prisma/schema.prisma): Define the Category model with id, name, and createdAt.  Seed Script (server/prisma/seed.ts): Write an idempotent seed script using Prisma Client's upsert method to insert the four required request categories:  'Account and Access'  'Hardware'  'Software'  'Network'  Package Configuration (server/package.json): Configure the "prisma": { "seed": "..." } script so that npx prisma db seed runs seamlessly.Ensure no hardcoded database credentials or environment variables are added to the tracked code. | The agent properly created the Category schema and seed.ts using upsert. I configured my PostgreSQL service locally, executed the migration, and verified that repeated seeding didn't create duplicate records. |
| 5 | Implement Issue 4: Display the IT request category list for TokTickIT according to the Lab 1 specifications:Backend (server/): Create a GET /api/categories endpoint in Express that queries the Category table from PostgreSQL using Prisma Client, ordered by id ascending, and returns JSON array of objects with id and name.  Backend Testing (server/tests/lab-01/categories.test.ts): Write a Supertest test (named API-02) verifying that GET /api/categories returns HTTP 200 and the 4 seeded categories.  Frontend (client/src/): Update api.ts and App.tsx with Bootstrap styling. When clicking the [Check System] button, show loading state, call both /api/health and /api/categories, and render:  App title: 'TokTickIT IT Service Desk'  'System Status: Online'  'Supported Request Categories:' followed by the 4 items (Account and Access, Hardware, Software, Network).  Proper error handling: If the backend or DB is offline, display an informative error message (e.g., 'System Status: Offline / Unable to connect to TokTickIT API').  Frontend Testing (client/tests/lab-01/App.test.tsx): Write Vitest tests with mocks covering:  UI-01: TokTickIT heading renders properly.  UI-02: Loading state successfully transitions to displaying the category list.  UI-03: API failure displays a useful error message.  Do not add functionality beyond the Lab 1 scope. | The agent successfully implemented the endpoint and connected the React frontend with full loading/error state handling. I ran all Vitest and Supertest suites and verified that all required test cases passed cleanly. |

## Reflection
Providing concrete directory trees, exact Acceptance Criteria, and strict tool constraints in my prompts significantly improved the agent's output accuracy and prevented unnecessary boilerplate.
