# Lab 1 — AI Use and Reflection  (fill this in)

**LLM/agent used:** <name>

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 |  |  |
Read the enclosed TokTickIT Lab 1 requirements. 
Summarize the four GitHub Issues, their dependencies, 
required outputs, and required automated tests. Propose 
an implementation order, but do not write code yet. เป็นไทย แต่คำไหนเป็น keyword ให้คงไว้
| 2 |  |  |
Verify the TokTickIT project foundation setup across `client/` and `server/`. Ensure that `client/` builds correctly with React, TypeScript, and Bootstrap, and `server/` runs Express with Prisma initialized. Verify that Vitest and Supertest configurations are intact. Check that root `.gitignore` excludes `node_modules/`, `.env`, `dist/`, and `build/`. Do not add any new feature code outside of the Issue 1 foundation scope.
| 3 |  |  |

## Reflection
Two or three sentences: what made your prompts better, and one place you had to
correct or reject what the agent produced.
